import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EDEEEE',
    fontColor: '#3E1B16',
  },
  scrollView: {
    marginHorizontal: 10,
  },
});
