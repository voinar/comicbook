import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  thumbnailText: {
    fontSize: 18,
    marginBottom: 6,
  },
  thumbnailImage: {
    marginBottom: 10,
    borderWidth: 2,
    borderColor: '#3E1B16',
    borderRadius: 10,
    width: '100%',
    height: 300,
  },
});

export default styles;
